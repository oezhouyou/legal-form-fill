services:
  backend:
    build: ./backend
    env_file:
      - .env
    volumes:
      - uploads:/app/uploads
    expose:
      - "8000"

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend

volumes:
  uploads:
