# ============================================================
# Legal Form Fill — Environment Configuration
# ============================================================
# Copy this file to .env and fill in the required values.

# --- Required ------------------------------------------------

# Anthropic API key for Claude vision document extraction
ANTHROPIC_API_KEY=sk-ant-xxxxx

# --- Optional ------------------------------------------------

# Claude model used for extraction (default: claude-opus-4-6)
# CLAUDE_MODEL=claude-opus-4-6

# Max tokens for Claude API responses (default: 4096)
# CLAUDE_MAX_TOKENS=4096

# Target web form URL to auto-fill via Playwright
# TARGET_FORM_URL=https://mendrika-alma.github.io/form-submission/

# Allowed CORS origins (JSON array or comma-separated)
# CORS_ORIGINS=["http://localhost:5173","http://localhost:3000"]

# Maximum upload file size in megabytes (default: 20)
# MAX_FILE_SIZE_MB=20

# Playwright page-load timeout in milliseconds (default: 30000)
# PLAYWRIGHT_TIMEOUT_MS=30000

# Python logging level: DEBUG, INFO, WARNING, ERROR (default: INFO)
# LOG_LEVEL=INFO
